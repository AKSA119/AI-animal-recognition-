# 🐾 AI-Based Animal Recognition System (Online + Offline)

This application recognizes animals using the **Imagga API** (online) and provides an **offline backup** using a pretrained **MobileNetV2** model.

## 🚀 Features
- Upload or capture animal images.
- Online recognition via Imagga API.
- Offline recognition using MobileNetV2 if API fails.
- Simple and interactive Streamlit UI.

## ⚙️ Run the Application
```bash
pip install -r requirements.txt
streamlit run app.py
